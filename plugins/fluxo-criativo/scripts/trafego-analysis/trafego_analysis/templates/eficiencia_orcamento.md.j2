# 💰 Eficiência de Orçamento (Pareto 80/20) — {{ produto }}

> **Sofia — Analista de Performance RTG** · Método VTSD

**Período:** {{ periodo.since.strftime('%d/%m/%Y') }} a {{ periodo.until.strftime('%d/%m/%Y') }} ({{ periodo.days }} dias)
**Gasto total:** {{ total_spend | brl }} · **Resultados:** {{ total_results | num }}

---

## Pareto 80/20

| Segmento | Conjuntos | Share Spend | Share Results |
|---|---|---|---|
{% if top20_count %}
| Top 20% | {{ top20_count }} | {{ top20_spend_share | pct }} | {{ top20_results_share | pct }} |
{% endif %}
{% if bottom_count %}
| Bottom 50% | {{ bottom_count }} | {{ bottom_spend_share | pct }} | {{ bottom_results_share | pct }} |
{% endif %}

{% if desperdicio_pct > 0 %}
**🔴 Desperdício identificado:** {{ desperdicio | brl }} ({{ desperdicio_pct | pct }} do budget) em conjuntos com zero resultados.
{% endif %}

---

## 🎯 Insights VTSD

{% for insight in insights %}
- {{ insight }}
{% endfor %}

---

## Ranking completo (por resultados)

| # | Conjunto | Gasto | Resultados | CPL/CPA | Share Spend | Veredicto |
|---|---|---|---|---|---|---|
{% for i in items[:25] -%}
| {{ loop.index }} | `{{ i.nome }}` | {{ i.spend | brl }} | {{ i.results | num }} | {{ i.cpl | brl }} | {{ i.share_spend | pct }} | **{{ i.veredicto | upper }}** |
{% endfor %}
{% if items | length > 25 %}
*...e mais {{ (items | length) - 25 }} conjuntos.*
{% endif %}

---

## Regras VTSD de concentração

| Fase | Máx 1 conjunto |
|---|---|
| **Caixa Rápido** | 70% (concentrar no winner) |
| **Pico de Vendas** | 40% (balancear HOT + COLD) |
| **Evergreen** | 50% (evitar dependência) |

Se algum conjunto cruzou o limite da fase → **diversificar é prioridade de risco** (se esse conjunto cair, a conta cai junto).

---
*Sofia · Método VTSD · Ready To Go*
